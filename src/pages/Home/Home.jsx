import { NavBar } from "./NavBar/NavBar";

export const Home = ({ recipes }) => {
  // const { result } = recipes;

  // if (result) {
  //   const { hits } = result;
  //   console.log(hits);

  return (
    <>
      <NavBar />
    </>
  );
};
